# 2026-02-16 - AFFiNE Installation Status Review

## 04:00 - AFFiNE Installation Status Summary

Nathan requested a comprehensive review of the AFFiNE self-hosted installation. Here's what we found:

### ✅ COMPLETED

1. **Docker Installation**
   - Docker Desktop installed (version 29.2.0)
   - Docker Compose configuration created at `/Volumes/ziggy/affine/`
   
2. **AFFiNE Stack Configuration**
   - `docker-compose.yml` properly configured with:
     - AFFiNE server (port 3010)
     - PostgreSQL database (pgvector/pg16)
     - Redis cache
     - Migration job
   - All data stored on 2TB external SSD at `/Volumes/ziggy/affine/`
   - Database credentials set (DB: affine, user: affine)
   
3. **Cloudflare Tunnel Setup** ✨
   - Tunnel created: `affine-pkm-tunnel` (ID: b7f766ae-65f5-41ae-b25d-3685dd0ada2d)
   - Active with 4 connections (2xatl01, 1xatl13, 1xatl14)
   - Config at `~/.cloudflared/config.yml`
   - Routes `pkm.mouthygeese.com` → `http://localhost:3010`
   - Tunnel process running (PID 945)
   
4. **DNS Configuration**
   - `pkm.mouthygeese.com` resolving to Cloudflare IPs (104.21.46.34, 172.67.223.34)
   - HTTPS working via Cloudflare proxy
   
5. **Service Status** (as of 04:00)
   - Docker containers running:
     - `affine_server` - Up and healthy on port 3010
     - `affine_postgres` - Up and healthy
     - `affine_redis` - Up and healthy
   - AFFiNE accessible at https://pkm.mouthygeese.com (HTTP 200 ✅)

### ⚠️ NEEDS ATTENTION

1. **Docker Auto-Start**
   - Docker Desktop must be manually launched after Mac restart
   - Containers are NOT set to auto-start on boot
   - **Fix:** Configure Docker Desktop to start on login + set containers to restart policy

2. **Cloudflare Tunnel Auto-Start**
   - Tunnel process running manually (not via launchd service)
   - Will not survive Mac restart
   - LaunchAgent plist exists but not loaded: `~/Library/LaunchAgents/homebrew.mxcl.cloudflared.plist`
   - **Fix:** `launchctl load ~/Library/LaunchAgents/homebrew.mxcl.cloudflared.plist`

3. **Domain Config in .env**
   - Domain variables still commented out in `/Volumes/ziggy/affine/.env`:
     ```
     # AFFINE_SERVER_HTTPS=true
     # AFFINE_SERVER_HOST=mouthygeese.com
     # AFFINE_SERVER_EXTERNAL_URL=https://mouthygeese.com
     ```
   - These should be uncommented and updated to use `pkm.mouthygeese.com`
   - **Impact:** AFFiNE might generate incorrect URLs internally

4. **Base Domain Not Configured**
   - `mouthygeese.com` (root domain) returns "No answer" from DNS
   - Only subdomain `pkm.mouthygeese.com` is configured
   - **Decision needed:** Should root domain point somewhere or stay unresolved?

### 🎯 NEXT STEPS (Priority Order)

1. **Uncomment and fix domain config** in `.env`:
   ```bash
   AFFINE_SERVER_HTTPS=true
   AFFINE_SERVER_HOST=pkm.mouthygeese.com
   AFFINE_SERVER_EXTERNAL_URL=https://pkm.mouthygeese.com
   ```
   Then restart containers: `cd /Volumes/ziggy/affine && docker compose restart`

2. **Set up Cloudflare Tunnel auto-start**:
   ```bash
   launchctl load ~/Library/LaunchAgents/homebrew.mxcl.cloudflared.plist
   launchctl start homebrew.mxcl.cloudflared
   ```

3. **Configure Docker Desktop auto-start**:
   - Docker Desktop → Settings → General → "Start Docker Desktop when you log in" ✅
   - Ensure containers have `restart: unless-stopped` (already done in docker-compose.yml ✅)

4. **Test full reboot** to confirm everything comes back up automatically

### 📊 CURRENT STATUS

**Overall:** 🟡 **Functional but not production-ready**
- AFFiNE is accessible and working at https://pkm.mouthygeese.com
- But requires manual intervention after Mac restart
- Domain config needs updating to avoid potential URL issues

**Blockers:** None - service is live and accessible right now
**Risk:** Low - data is safe on external SSD, but service won't survive reboot without fixes
